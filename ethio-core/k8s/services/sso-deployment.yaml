apiVersion: apps/v1
kind: Deployment
metadata:
  name: sso-service
  namespace: ethio-core
  labels:
    app: sso-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sso-service
  template:
    metadata:
      labels:
        app: sso-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8006"
    spec:
      serviceAccountName: ethio-core-sa
      containers:
        - name: sso-service
          image: ethio-core/sso-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8006
              name: http
          envFrom:
            - configMapRef:
                name: ethio-core-config
            - secretRef:
                name: ethio-core-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sso-service
  namespace: ethio-core
spec:
  selector:
    app: sso-service
  ports:
    - port: 8006
      targetPort: 8006
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sso-service-hpa
  namespace: ethio-core
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sso-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
