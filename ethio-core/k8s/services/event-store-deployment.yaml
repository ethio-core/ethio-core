apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-store-service
  namespace: ethio-core
  labels:
    app: event-store-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: event-store-service
  template:
    metadata:
      labels:
        app: event-store-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8010"
    spec:
      serviceAccountName: ethio-core-sa
      containers:
        - name: event-store-service
          image: ethio-core/event-store:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8010
              name: http
          envFrom:
            - configMapRef:
                name: ethio-core-config
            - secretRef:
                name: ethio-core-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8010
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8010
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: event-store-service
  namespace: ethio-core
spec:
  selector:
    app: event-store-service
  ports:
    - port: 8010
      targetPort: 8010
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: event-store-service-hpa
  namespace: ethio-core
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: event-store-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
